/** Apply current / visited / considered styles (stub). */
export type HighlightState = "current" | "visited" | "considered";

export function nodeHighlightClass(_state: HighlightState): string {
  return "";
}
